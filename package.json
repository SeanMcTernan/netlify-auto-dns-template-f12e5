{
  "name": "netlify-auto-dns-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Scheduled Netlify function that auto-assigns custom domains to new sites on a team.",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "netlify database migrations apply"
  },
  "dependencies": {
    "@netlify/database": "^1.1.0",
    "@netlify/functions": "^2.8.0",
    "drizzle-orm": "^1.0.0-beta.22"
  },
  "devDependencies": {
    "drizzle-kit": "^1.0.0-beta.22",
    "netlify-cli": "^17.0.0",
    "typescript": "^5.4.0"
  }
}
